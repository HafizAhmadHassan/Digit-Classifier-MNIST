# MNIST Digit Classifier (from scratch)

A handwritten digit classifier built entirely from scratch in MATLAB. Implements an artificial neural network with a perceptron layer, sigmoid activation, and MNIST data loading, then trains and tests on the MNIST dataset.
